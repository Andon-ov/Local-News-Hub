import HeroCarousel from './Carousel';

function Hero() {

    return (
        <section className="hero">
            <h2 className="heroTitle">Hot Topics</h2>
            <HeroCarousel/>
        </section>
    );
}

export default Hero;
